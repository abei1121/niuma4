[package]
name = "gemini_account_probe"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "gemini_account_probe"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
